
import { computeSelection } from 'vimeauto/helpers/compute-selection';
import { module, test } from 'qunit';

module('Unit | Helper | compute selection');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = computeSelection([42]);
  assert.ok(result);
});

